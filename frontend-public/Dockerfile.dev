# Frontend Public Dockerfile for Local Development
FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json ./
RUN npm install

# Expose Vite dev server port
EXPOSE 3000

# Use Vite dev server with hot-reload
CMD ["npm", "run", "dev"]
